interface:
  display_name: Design
  short_description: Curated design, detector, and interface review router
  brand_color: "#7C3AED"
  default_prompt: Use $design for a controlled design or interface change review workflow.
policy:
  allow_implicit_invocation: false
