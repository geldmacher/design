interface:
  display_name: "Update Motion"
  short_description: "Update the pinned free Motion integration"
  default_prompt: "Use $update-motion to review and update the pinned free Motion integration."
policy:
  allow_implicit_invocation: false
