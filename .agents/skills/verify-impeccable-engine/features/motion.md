# Motion integration

Run from the source checkout with Node.js 22+, Git, zip, unzip and installed dependencies. No network, server, account or host installation is needed. The existing verifier runs `tests/motion-maintenance.test.mjs` and `tests/motion-contract.test.mjs` in focused and full mode.

Before driving, inspect the package scripts, Motion pin, the current CLI contracts and Git state. The tests build isolated archives and projects, exercise the real candidate import and apply functions, and verify byte-identical outputs, archive/anchor/inventory rejection, candidate drift and rollback. Package trials build all three targets and inspect their real manifests, resources and status output from a foreign working directory.

Controlled response fixtures cover free documentation/example resource links, premium-only responses, missing tools, missing resource reader and network errors. These establish the packaged guidance contract, not that an actual agent follows it. Host-smoke guidance separately drives search, resource reads, fallback and refusal of response-based scope expansion in fresh Cursor and Codex sessions after separately authorized installation.

Expected observations: only the anonymous endpoint is packaged natively; the portable target contains local guidance without an MCP declaration. No paid capability, automatic migration, install or login workflow is offered. Offline status reports availability as not checked. Corrupted inputs fail before destination writes; apply failures restore the prior owned bytes. Unrelated project and home sentinels remain unchanged.

At implementation close run `npm run release-check` and `git diff --check`. Inspect the exact TAP transcript and `result.json`, including Motion pin, source hashes, selected tests, exit status and cleanup. A failing case blocks repository acceptance; a missing fresh host trial leaves runtime support unconfirmed. Retain evidence outside temporary fixtures after cleanup. Do not change expected behavior to accept a failure.
